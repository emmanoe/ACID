function [ incorrects ] = FindIncorrects( descriptors, line )
    % FINDINCORRECTS
    %
    % Finds and return the descriptors that are on the negative side of the
    % given line.
    %
    % Arguments:
    %     descriptors (Matrix): Matrix containing the descriptors to project
    %     line         (Array): Coeficient of the line equation defined as
    %                           y = -( line(1) + x*line(2) ) / line(3)
    %
    % Returns:
    %     incorrects (Matrix): Descriptors that are on the negative of the
    %                          line
    %
    
    error( 'Fonction a implémenter' );
    
    % Project the descriptors using the line to find their distance to it
    % projected =
    
    % Indexes of the ones on the negative side of the line
    % indexes = 
    
    % Fetch the value of the descriptors at thoes indexes
    % incorrects = 
end

