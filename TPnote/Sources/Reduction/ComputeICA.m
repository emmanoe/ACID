function [ W ] = ComputeICA( split, dims )
%COMPUTEICA

counts      = split.counts;
descriptors = split.descriptors;

error( 'Fonction a implémenter' );

% Compute covariance matrix sum and inverse it
% cov_1   = 
% cov_2   = 
% sum_cov = 
% iversed_sum_cov = 

% Compute independent means, and global mean
% mean_1 = 
% mean_2 = 
% datas = 
% datas_mean = 

% Compute Sb
% Sb = 

% Compute eigen vectors and egein values
% [eigen_vectors, eigen_values] = 

% Sort egein values
% [~, indices] = 

% Get the dims first 
% indices =
% W =
end

